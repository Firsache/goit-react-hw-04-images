import React from 'react';
import ReactDOM from 'react-dom/client';
import { Gallery } from 'components/App/App.jsx';
import 'styles/index.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <Gallery />
  </React.StrictMode>
);
